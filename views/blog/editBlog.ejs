<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Post</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<%- include('../nav.ejs'); %>
<body class="bg-gray-50 dark:bg-gray-900">
  <div class="max-w-3xl mx-auto p-8 bg-white shadow-md mt-10 rounded-lg">
    <h1 class="text-2xl font-bold mb-6 dark:text-white">Edit Your Blog Post</h1>
    
    <form action="/edit/<%= data.id %>" method="POST" class="space-y-4">
      
      <div>
        <label class="block font-semibold text-gray-700 dark:text-white">Title</label>
        <input type="text" name="title" value="<%= data.title %>"
               class="w-full px-4 py-2 border rounded-md dark:bg-gray-800 dark:text-white" required />
      </div>

      <div>
        <label class="block font-semibold text-gray-700 dark:text-white">Description</label>
        <textarea name="description" rows="2"
                  class="w-full px-4 py-2 border rounded-md dark:bg-gray-800 dark:text-white" required><%= data.description %></textarea>
      </div>

      <div>
        <label class="block font-semibold text-gray-700 dark:text-white">Content</label>
        <textarea name="content" rows="10"
                  class="w-full px-4 py-2 border rounded-md dark:bg-gray-800 dark:text-white" required><%= data.content %></textarea>
      </div>

      <button type="submit"
              class="bg-blue-600 text-white px-6 py-2 rounded-md hover:bg-blue-700 transition">
        Save Changes
      </button>
    </form>
  </div>
</body>
</html>
